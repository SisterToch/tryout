#ifndef MAIN_H
#define MAIN_H

#include <stddef.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

ssize_t _getline(char **lineptr, size_t *n);
char *_strtok(char *string, char *delimiter)

#endif
